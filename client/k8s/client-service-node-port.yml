apiVersion: v1
kind: Service
metadata:
  name: client-service
spec:
  selector:
    app: client
  type: NodePort
  ports:
    - name: client
      protocol: TCP
      port: 3000
      targetPort: 3000
